"""Preflight — lightweight plan review gate (Epic 28).

Evaluates implementation plans between Assembler (step 5) and Primary Agent
(step 6) to catch bad plans before they waste implementation compute.
"""
